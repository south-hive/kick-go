{
  "name": "wood-and-stone",
  "private": true,
  "version": "1.4.0",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "playwright test",
    "build:release": "node scripts/stamp-release.js"
  },
  "dependencies": {
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  },
  "engines": {
    "node": ">=20"
  }
}
